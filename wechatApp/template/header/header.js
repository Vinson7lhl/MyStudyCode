//logs.js
// const util = require('../../utils/util.js')

/**
 * 在template中可以没有js，仅仅是template的片段。然后在使用的地方导入即可，它的作用仅仅是纯粹的view片段，不应该有业务逻辑。
 * 1、可复用的view片段
 * 2、无业务逻辑
 */

// Page({
//   // 初始元数据
//   data: {
//   }
// })
